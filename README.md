# Machine-Learning-NTI-Final-Project
I created a RealEstate Machine Learning Prediction model Using (Pandas to call the DataSet), than started the PreProcessing journey through changing the names of column Numbers and Categorical objects, than looked for any null values and filled them with Median(Numerical) and Mode(Categorical)/ finishing with the models (DecisionTreeClassifier)
